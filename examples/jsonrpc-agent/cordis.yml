# Unattended coding-agent deployment for the bundled toh-jsonrpc-agent runtime.
# stdout is reserved for JSON-RPC; do not add a console logger or terminal UI.

- id: sdk-jsonrpc-server
  name: '@buckeyestudio/toh-sdk-jsonrpc-server'
  config:
    maxTokensAsSuccess: !!js "process.env.TOH_MAX_TOKENS_AS_SUCCESS === undefined ? true : JSON.parse(process.env.TOH_MAX_TOKENS_AS_SUCCESS)"

# The DeepSeek adapter. Shipped default: full thinking at max effort on every
# request; exact-model resolution materializes request defaults before logging.
# The model arrives per session over JSON-RPC, so it is not pinned here.
- id: llm-deepseek
  name: '@buckeyestudio/toh-llm-deepseek'
  config:
    thinking: enabled
    reasoningEffort: max

# Managed child-process groups for the bash executor (spawn/kill/output plumbing).
- id: subprocess
  name: '@buckeyestudio/toh-subprocess-local'

- id: bash
  name: '@buckeyestudio/toh-bash-local'
  config:
    cwd: !!js process.env.TOH_CWD ?? process.cwd()
    timeoutMs: 60000

- id: agent-spine
  name: '@buckeyestudio/toh-agent-spine-demo'
  config:
    persona: !!js process.env.TOH_SYSTEM_PROMPT ?? 'You are a coding agent.'
    workspaceContext: false
    skills:
      enabled: false
    toolBash:
      enableRunInBackground: false
    toolJobs: false

- id: sessions
  name: '@buckeyestudio/toh-session-persistence-jsonl'
  config:
    root: !!js process.env.TOH_SESSION_ROOT ?? './.sessions'
    # Snapshot runs read the raw JSONL back; production keeps zstd frames.
    compression: !!js "process.env.TOH_SNAPSHOT === undefined ? 'zstd' : 'none'"

- id: session-checkpoints
  name: '@buckeyestudio/toh-session-checkpoint-policy'

- id: subagent
  name: '@buckeyestudio/toh-subagent'

- id: subagent-spawn-in-process
  name: '@buckeyestudio/toh-subagent-spawn-in-process'
  config:
    providerName: spawn

- id: tool-subagent
  name: '@buckeyestudio/toh-tool-subagent'
  config:
    provider: spawn
    toolName: subagent
    enableRunInBackground: false

- id: tool-todo
  name: '@buckeyestudio/toh-tool-todo'
  config:
    allowParallelInProgress: true

- id: fs-local
  name: '@buckeyestudio/toh-fs-local'
  config:
    cwd: !!js process.env.TOH_CWD ?? process.cwd()

- id: fs-observation-policy
  name: '@buckeyestudio/toh-fs-observation-policy'

- id: tool-fs
  name: '@buckeyestudio/toh-tool-fs'

- id: token-meter
  name: '@buckeyestudio/toh-token-meter'

- id: compaction-basic
  name: '@buckeyestudio/toh-compaction-basic'
  config:
    thresholdRatio: 0.8
    retainRatio: 0.16
    maxTokens: 8192
    compactionRetries: 1
