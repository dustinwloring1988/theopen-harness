# Keyless real-Loader composition for the semantic-checkpoint recovery snapshot.
# The headless driver resumes the seeded interrupted session and emits its next
# turn over stream-json; the replay adapter supplies the deterministic response.

- id: persistence
  name: '@buckeyestudio/toh-session-persistence-jsonl'
  config:
    root: './.sessions'
    compression: none

- id: checkpoint
  name: '@buckeyestudio/toh-session-checkpoint-policy'

- id: replay
  name: '@buckeyestudio/toh-llm-replay'
  config:
    file: !!js process.env.TOH_SNAPSHOT_FILE
    overrideFile: !!js process.env.TOH_SNAPSHOT_OVERRIDE

# Managed child-process groups for the bash executor (spawn/kill/output plumbing).
- id: subprocess
  name: '@buckeyestudio/toh-subprocess-local'

- id: bash
  name: '@buckeyestudio/toh-bash-local'
  config:
    timeoutMs: 60000

- id: agent
  name: '@buckeyestudio/toh-agent-spine-demo'
  config:
    agents: []
    workspaceContext: false
    skills:
      enabled: false
    toolJobs: false
    goals: false

# Await the persisted resume before the headless driver inspects root agents.
- id: resumed-agent
  name: './tests/fixtures/semantic-checkpoint-agent.ts'
