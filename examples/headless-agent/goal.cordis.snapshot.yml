# Replay counterpart to goal.cordis.yml. It includes cordis.yml directly because
# a config patch cannot target an entry behind a nested include, then restates
# the goal overlay while replacing the live model with keyless replay.
- id: base
  name: '@buckeyestudio/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - id: llm-deepseek
        name: '@buckeyestudio/toh-llm-deepseek'
        disabled: true
      - insert:
          - id: goal
            name: '@buckeyestudio/toh-goal'
          - id: tool-goal
            name: '@buckeyestudio/toh-tool-goal'
          - id: llm-replay
            name: '@buckeyestudio/toh-llm-replay'
