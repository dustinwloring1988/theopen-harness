# Keyless replay counterpart to web.cordis.yml: the web stack and loopback
# fixture server stay real (the tool call re-executes the actual HTTP fetch and
# markdown rendering); only the model adapter is replaced by replay.
- id: base
  name: '@buckeyestudio/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - id: llm-deepseek
        name: '@buckeyestudio/toh-llm-deepseek'
        disabled: true
      - insert:
          - id: web
            name: '@buckeyestudio/toh-web'
          - id: web-fetch-http
            name: '@buckeyestudio/toh-web-fetch-http'
            # The fixture target is loopback by design, so this scenario opts
            # into the provider's private-network guard.
            config:
              allowPrivateNetworks: true
          - id: web-fetch-fixture
            name: './web-fetch-fixture-server.mjs'
          - id: tool-web
            name: '@buckeyestudio/toh-tool-web'
            config:
              search: false
          - id: llm-replay
            name: '@buckeyestudio/toh-llm-replay'
            config:
              providers:
                - id: deepseek-official
                  name: DeepSeek
                  models:
                    - id: deepseek-v4-flash
                    - id: deepseek-v4-pro
