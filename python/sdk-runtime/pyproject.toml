[build-system]
requires = ["hatchling==1.30.1"]
build-backend = "hatchling.build"

[project]
name = "theopen-harness-runtime-bin"
version = "0.0.0.dev0"
description = "Pinned TheOpen Harness runtime for the Python SDK"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [{ name = "buckeyestudio" }]

[project.urls]
Homepage = "https://github.com/dustinwloring1988/theopen-harness"
Documentation = "https://github.com/dustinwloring1988/theopen-harness/blob/master/python/sdk-runtime/README.md"
Issues = "https://github.com/dustinwloring1988/theopen-harness/issues"
Source = "https://github.com/dustinwloring1988/theopen-harness"

# Include the injected executable and default config; exclude the dev-only node
# closure from wheels and sdists.
[tool.hatch.build]
artifacts = ["src/theopen_harness_runtime/runtime/toh-jsonrpc-agent-*"]
exclude = ["src/theopen_harness_runtime/runtime/node"]

[tool.hatch.build.targets.wheel]
packages = ["src/theopen_harness_runtime"]

[tool.hatch.build.targets.wheel.hooks.custom]

[tool.hatch.build.targets.sdist.hooks.custom]
