/**
 * @buckeyestudio/toh-base — the shared toh core as a profile bundle. The
 * package's substance is `cordis.patch.yml`, declared by the `toh.bundle.patch`
 * manifest field and resolved by the profile composer through that field;
 * this module carries no runtime API.
 * @module @buckeyestudio/toh-base
 */

export {}
